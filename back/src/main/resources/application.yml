spring:
  datasource:
    url: jdbc:mysql://localhost:3306/autho_school
    username: root
    password: 3007
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.example.back_full: DEBUG
